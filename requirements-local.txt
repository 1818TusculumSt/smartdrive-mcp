# Optional: install only when EMBEDDING_PROVIDER=local.
# For CPU-only machines, install the CPU PyTorch wheel first:
# pip install --index-url https://download.pytorch.org/whl/cpu torch
sentence-transformers
